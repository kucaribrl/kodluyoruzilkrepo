# IQ Basics — `claude/textile-wholesale-app-4zqrky` dalı inceleme raporu

Tarih: 2026-09-03 · İncelenen commit: `e6d0775` (main'in 130 commit ilerisinde, main ile çakışma yok)

## Özet

- **Testler:** `test/*.test.mjs` altı dosyanın tamamı bu dalda Chromium ile koşuldu, **6/6 geçti** (121 PASS, 0 FAIL, sayfa hatası yok).
- **Yöntem:** index.html (7.522 satır, 825 KB) bölge bölge okundu; Firestore/Storage kuralları, sw.js, manifest, yazıcı ajanı, WhatsApp ajanı ve testler ayrıca incelendi. Aşağıdaki her bulgu kodda satır satır doğrulandı; doğrulanamayan iddialar rapora alınmadı.
- **Genel kanı:** Uygulama işlevsel olarak çok geniş ve önceki inceleme turunda (703edb3..e6d0775) ciddi düzeltmeler yapılmış. Kalan en büyük risk alanı **çok cihazlı senkron + demo veri + cihaz temizleme** üçgeni: birkaç sık kullanılan akış (Sıfırla, Güvenli Çıkış, yeni cihaz ekleme) buluttaki gerçek veriyi silebiliyor ya da sahte demo verisiyle kirletebiliyor. İkinci risk alanı **yetki kapısı**: "para görür" izni fiilen yönetici yetkisi gibi çalışıyor. Üçüncüsü **portaldan gelen sipariş belgelerine güvenilmesi** (XSS + yanlış cariye borç).

Önem dereceleri: **kritik** = normal kullanımda para/veri kaybı veya güvenlik ihlali · **yüksek** = sık akışta yanlış sonuç/çökme · **orta** = uç durum / bozulmuş davranış · **düşük** = küçük ama gerçek.

---

## 1. KRİTİK

### K1. `sifirla()` canlı senkron açıkken buluttaki gerçek veriyi mezar taşlarıyla siliyor — `index.html:1274`
- **Mekanizma:** `sifirla()` → `canliDur()` → `db=seed(); db.canliBulut=false; save()`. `save()` içindeki `_damgala()` (612-621) bir önceki `_mSnap`'i (gerçek kayıtlar) yeni seed dizileriyle kıyaslar ve **gerçek kayıtların tamamına `_tomb` mezar taşı yazar**; `_mSnapInit()` çağrılmaz (yedekten dönüşte 880/7416'da çağrılıyor, burada unutulmuş). Sayfa yenilenince 7457'deki açılış kodu `otoSenkron()`'u **`db.canliBulut=false` bayrağına bakmadan** çağırır (700-716 yalnız `bulutGirisli()`'ye bakar). `canliDomenUygula` mezar taşlarını `_tombMerge` ile birleştirir, `_recMerge` (603) `td>=r._m` koşuluyla buluttaki gerçek kayıtları düşürür; 680'de `db.ornek=false` olur ve 687'deki "yerel katkı" dalı mezar taşlarını + demo kayıtları `canliYazHemen` ile buluta yazar. Diğer cihazlar da bu mezar taşlarını alıp kendi kayıtlarını siler; 30 gün boyunca yedekten dönseler bile tekrar silinir.
- **Onay metni** "buluttaki gerçek veri EZİLMEZ" diyor; doğru değil.
- **Öneri:** `db=seed()` sonrasında `delete db._tomb; _mSnapInit();` (save'den önce). `otoSenkron()` ve açılış (7457) `db.canliBulut===false` ya da `db.ornek===true` iken otomatik bağlanmasın; `canliBaslat()` ilk snapshot uygulanmadan `db.ornek=false` yapmasın ve demo cihazdan 687'deki geri yayın yapılmasın.

### K2. "Güvenli Çıkış" / "Çıkış yap ve temizle" IndexedDB kopyasını silmiyor; veri yeniden açılışta geri geliyor — `index.html:899-906`, `896`
- **Mekanizma:** `_cihazTemizle()` ve `personelCik()` yalnız `localStorage` anahtarlarını (`KEY`, `iq_yedekler`, `oz_portal_oturum`) siliyor. Açılıştaki IIFE (646-655) `mevcut=null` olunca **koşulsuz** IndexedDB kopyasını alıp `db=d` yapıyor, localStorage'a geri yazıyor ve "💾 IndexedDB güvence kopyasından geri yüklendi ✓" gösteriyor. Sonuç: temizlendiği söylenen personel cihazında tüm cariler/bakiyeler/satışlar giriş yapılmadan (personelCik'te oturum kilidi de yok) tekrar görünür.
- **Öneri:** `idbSil()` ekleyin (`indexedDB.deleteDatabase('iqbasics')` ya da kv store `delete(KEY)`) ve `personelCik`, `_cihazTemizle`, istenirse `bosBaslat` içinde çağırın; temizlik sonrası bir `iq_temizlendi`/`iq_oturumKilit` bayrağı varken IIFE IDB'den geri yüklemesin.

### K3. Oturum kilidi HERHANGİ bir Firebase hesabıyla açılıyor (K2 ile birleşince tam veri sızıntısı) — `index.html:1451-1456`
- **Mekanizma:** `oturumKilitGiris()` yalnız `signInWithEmailAndPassword` başarısını şart koşuyor; hesabın bu işletmenin onaylı personeli olup olmadığına bakmadan `localStorage.removeItem('iq_oturumKilit')` yapıyor. Portalda herkese açık "Kayıt Ol" var ve `firestore.rules` `rol in ['musteri','fason']` ile herkesin kendine hesap açmasına izin veriyor. Ödünç/ortak cihazda "Güvenli Çıkış" yapılmış olsa bile cihaz sahibi kendine müşteri hesabı açıp kilidi geçer; K2 nedeniyle IDB'den geri yüklenen db ile patron arayüzü ve tüm veri karşısına çıkar.
- **Öneri:** Kilidi kaldırmadan önce `bulutKul && bulutKul.onay && ['admin','ortak','magaza','depocu','fasontakip'].includes(bulutKul.rol)` (ya da `SAHIP_MAILLER`) şartı; sağlanmazsa `signOut()` + hata mesajı.

### K4. Yeni cihaz dolu buluta katılınca DEMO (seed) kayıtları buluta ve tüm cihazlara karışıyor — `index.html:679-687`, `700-716`
- **Mekanizma:** `otoSenkron` yalnız "bulut boşsa yükleme" kararında `db.ornek`'e bakıyor; bulut doluysa doğrudan `canliBaslat()`. `canliDomenUygula → _recMerge` bulutta olmayan yerel kayıtları (seed: ürün 1-5, cariler 1-3/101-102/201-204/301-302 — 435.000 ₺ bakiyeli "Yılmaz Tekstil" dahil —, hesaplar 1-4 toplam 852.500 ₺ sahte kasa/banka, demo satış, 300.000 ₺ demo çek, demo iş emri, demo bekleyen sipariş) "yerel katkı" sayar; 680'de `db.ornek=false` olduğu için 687'deki `canliYazHemen` artık engellenmez ve demo kayıtlar buluta, oradan tüm cihazlara yazılır. Kurulumdan 30 gün sonra (mezar taşları budanınca) eklenen **her** yeni cihaz bunu yapar. `canliBulutKatil` metni "bu cihazdaki … bununla değişecek" der ama kod değiştirmez, birleştirir.
- **Öneri:** `canliBaslat()` öncesinde `db.ornek===true` ise `MERGE_LISTELER` dizilerini ve `seq`'i boşaltın; seed kayıtlarına `_demo:true` koyup `_recMerge`/`canliYaz`'da asla buluta yazmayın; `db.ornek=false` atamasını geri yayından sonra yapın.

### K5. Kayıt bazlı "son yazan kazanır" birleştirme kümülatif bakiye/stok alanlarında para hareketini siliyor — `index.html:600-603`
- **Mekanizma:** `_recMerge` aynı id'de yalnız yüksek `_m`'li nesneyi tutar. `cariler.bakiye`, `hesaplar.bakiye`, `urunler.stok/renkler[].stok` ise yerinde artırılıp azaltılan alanlardır. İki cihaz aynı cariye/hesaba/ürüne yakın zamanda (450 ms debounce + gecikme) ya da biri çevrimdışıyken dokunduğunda kaybeden tarafın **tüm** bakiye/stok değişimi sessizce yok olur; satış/hareket kayıtları (farklı id) kalır, bakiye defterden ayrışır. Uygulama çevrimdışı çalışmayı açıkça destekliyor (enablePersistence, 924-925 toast'ları), bu yüzden gerçek senaryodur.
- **Örnek:** Kasa PC çevrimiçi: cari 100.000→120.000 (veresiye satış, t1). Patron telefonu çevrimdışı: aynı cari 100.000→70.000 (tahsilat, t2>t1). Telefon bağlanınca bakiye 70.000; doğrusu 90.000. 20.000 ₺ alacak kayboldu.
- **Öneri:** Bakiye/stok gibi türetilebilir alanları LWW ile taşımayın: ya hareket/satış/ödeme defterinden yeniden hesaplayın (cariEkstre/defter mantığı zaten var) ya da birleştirmede delta uygulayın; en azından çakışma (iki tarafta da `_m > snapshot`) tespit edilince uyarı + defterden yeniden hesap önerin.

### K6. Portal onayında kupon indirimi müşteri beyanıyla alınıyor → fiyat güvencesi tamamen atlanıyor, negatif tutar bile onaylanıyor — `index.html:5847-5848`, `5856-5860`
- **Mekanizma:** Sipariş belgesi müşteri tarafından `{...o}` olarak yazılıyor (1024); kurallar alanları doğrulamıyor. `portalOnay` `kuponInd`'i doğrudan `o.kupon.indirim`'den okuyor (`kuponBul`, `minTutar`, `bitis`, `limit` doğrulaması yok). `beklenen=Math.max(0,katalogT-kuponInd)` sıfıra düşünce `beklenen>0` koşulu fark kontrolünü **tamamen atlıyor** ve 5860'ta `tutar=override||o.tutar` müşterinin yazdığı tutarı aynen satışa geçiriyor. `+o.tutar>0` koşulu negatif tutarı da geçiriyor → `applySatis` müşteri carisine **alacak** yazıyor ve bu bakiye `musteri_veri` yayınıyla müşteriye gidiyor.
- **Senaryo:** Konsoldan `kupon:{kod:'X',indirim:9999999}, tutar:1, kalemler:[{uid:<gerçek>,mik:50,fiyat:1}]` → yönetici "Onayla"ya basar, uyarı çıkmaz, 50 seri 1 ₺'ye satılır. `tutar:-50000` ile müşteri 50.000 ₺ alacaklı görünür.
- **Öneri:** Kuponu yalnız `kuponBul(o.kupon.kod)` ile bulup indirimi `katalogT` üzerinden yeniden hesaplayın; fark kontrolünü `esle>0` ile sınırlayıp `beklenen` 0 olsa da çalıştırın; içeri alırken `tutar:Math.max(0,+o.tutar||0)`, `kalemler` alanlarını sayıya zorlayın; `tutar<=0` ise onaya izin vermeyin.

### K7. `pazarOlay` (müşteri davranış analitiği) belgesinden gelen alanlar yönetici ekranında `onclick`'e ve HTML'e ham giriyor — `index.html:6363-6367`, `6392`, `6396`, `6400-6403`
- **Mekanizma:** Kurallar giriş yapmış her kullanıcının `pazarOlay/{sid}` oluşturmasına izin veriyor (`kul==uid`, diğer alanlar serbest). `pazarAnalizCiz` `mid:s.mid||0` ve `m.kez+=g.kez||0` (string ise birleştirme) yapıyor, sonra `onclick="pazarMesaj(${m.mid})"` / `pazarSepetHatirlat(${m.mid})` ve `${m.kez} bakış` / `${p.kez} bakış` kaçışsız basıyor. Y5'teki `uid` sınıfının başka belge/sink'i; `gorunum:{"1":{kez:"<img src=x onerror=…>"}}` ile tıklamasız çalışır.
- **Öneri:** `mid=+s.mid||0`, `kez=+g.kez||0`, `sure=+g.sure||0`, `mik=+it.mik||0`; `onclick`'e yalnız sayısal id.

---

## 2. YÜKSEK

### Y1. Okuma hatası "bulut boş" sayılıp yerel veri buluta gönderiliyor — `index.html:706-712`
Dört `get()` çağrısı `.catch(()=>null)` ile yutuluyor; `d=null` → `dolu=false` → `bulutBos=true`. Hiç senkronlanmamış cihaz çevrimdışıyken (Firestore önbelleği boş → `unavailable`) ya da izin hatasında `canliGonder` tüm domenleri `set()` ile kuyruğa yazar; persistence açık olduğu için internet gelince sunucudaki belgeleri bu cihazın verisiyle değiştirir. id'li listeler diğer cihazlarca kısmen onarılır ama `ayar`, `kuponlar`, `stokBildirim`, `malKat` gibi tüm-belge alanlar kalıcı ezilir. **Öneri:** `get()` hatasında (özellikle `unavailable`/`permission-denied` ya da `metadata.fromCache`) yükleme yapmayın, yalnız dinleyin; `canliGonder`'a `db.ornek` koruması ekleyin; ilk yüklemeyi kullanıcı onayına bağlayın.

### Y2. "Para/finans görür" izni fiilen yönetici kapısı — `index.html:1478`, `1468-1472`, `7343-7351`, `1700`, `7364-7368`
`paraGoster()` şu yerlerde yönetici kontrolü gibi kullanılıyor: `rolIzinAyar`, `rolSec`, `rAyarlar` içindeki `kisitli=!paraGoster()` (→ "Kullanıcılar & Roller" bloğu, her rol için `onclick="setRol('${k}')"` satırları, PIN ayarı, Veri Güvenliği: yedek yükle / `bosBaslat` / `sifirla`), hesap menüsünde Güvenli Çıkış, `topluZam`. `setRol()` içinde `personelMod` engeli yok. Patron Rol İzinleri'nden mağazaya "para" verirse mağaza elemanı: (a) Rol İzinleri'ni açıp kendi rolüne her yetkiyi verebilir, (b) `setRol('patron')` ile patron arayüzüne geçer, (c) `bosBaslat()` ile canlı senkron açıkken tüm cihazlardaki stok/cari/satış/ayar kayıtlarını mezar taşıyla siler (kurallar mağazaya bu alanlara yazma izni veriyor). **Öneri:** Ayrı bir `adminMi()` kapısı (`db.rol==='patron' || (db.rol==='ortak' && !db.personelMod)` ya da `bulutKul.rol`) tanımlayıp rolIzinAyar/rolIzinSet/setRol/sifirla/bosBaslat/yedek yükleme/guvenliCikis/kilitAyar/topluZam bunu kullansın; `setRol` içinde `if(db.personelMod)return`.

### Y3. `rolIzinSet` yetki kontrolsüz ve `rolAyar` mağaza/fasontakip'in yazabildiği `ayar` belgesinde — `index.html:1499`, `firestore.rules:101`
Konsoldan `rolIzinSet('magaza','y','kar',true)` çağıran personel kendi izin matrisini değiştirir; `ayar` belgesine mağaza ve fasontakip yazabildiği için değişiklik buluta ve patronun cihazına yayılır. **Öneri:** `rolIzinSet` başında yönetici kontrolü; `rolAyar`'ı yalnız admin/ortak'ın yazabildiği ayrı bir domene taşıyın (kurallardaki `{domen}` yakalayıcı zaten admin/ortak).

### Y4. Portal siparişindeki `mid` müşteri kontrolünde; onayda borç istenen cariye yazılır — `index.html:1037`, `1250`, `5852-5865`
Bulut siparişi `mid:o.mid||0` ile aynen alınıyor; `portalOnay` önce `cari(o.mid)` ile eşleşiyor ve satış/borç o cariye işleniyor. Kurallar `mid`'i doğrulamıyor; istemci `o.uid → kullanicilar/{uid}.cariId` eşlemesini hiç kullanmıyor (bulut kullanıcı yönetiminde cari bağlanmasına rağmen). Cari id'leri küçük ardışık sayılar ve davet linkinde geçiyor. Yönetici ekranı (5789) saldırganın yazdığı `o.mus` adını gösterir. **Öneri:** Gelen `mid`'i yok sayın; `o.uid` ile `kullanicilar/{uid}` okuyup `cariId`'yi oradan bağlayın; ekranda `cari(o.mid).ad`'ı da gösterin.

### Y5. Portal sipariş belgesindeki alanlar doğrulanmadan yönetici ekranına basılıyor → saklı XSS — `index.html:5791`, `5808`, `1249/1036`
`firestore.rules:56` herhangi bir girişli hesabın `siparisler`'e serbest içerik yazmasına izin veriyor. `rPortal` 5791'de `k.kademeInd` ham basılıyor (`'%'+k.kademeInd+'↓'`); 5808'de `stokBildirim` içindeki `x.uid` (buluttan gelen `o.istekUid`, tür kontrolü yok) doğrudan `onclick="portalHaberVer(${uid})"` içine yazılıyor. Onaysız bile bir portal hesabı `kademeInd:'<img src=x onerror=…>'` ya da `istekUid:'1);fetch(…);('` gönderirse kod yönetici cihazında, yönetici Firebase oturumuyla çalışır. **Öneri:** Gelen siparişi şemaya göre temizleyin (`kalemler:{uid:+,urun:String,mik:+,birim:String,fiyat:+,kademeInd:+,renkler:[{ad:String,mik:+}]}`, `istekUid:+`); 5791'de `+k.kademeInd`, 5808'de `+uid`; kurallarda tipleri kısıtlayın.

### Y6. İki yönetici cihazı aynı portal siparişini farklı yerel id ile ekliyor → çift bekleyen, çift onay — `index.html:1250`, `5870`
`siparisDinle` yalnız yerel `siparisIslendiMi(doc.id)` kontrolüyle `nid('bp',false)` id'siyle ekler (id cihaz ekli olduğundan cihazlarda farklı). İki cihaz da birbirinin `satis` snapshot'ını almadan (450 ms debounce) eklerse `_recMerge` id'ye baktığı için aynı `bulutId`'li iki kayıt kalır; `canliSatisUzlas` yalnız işlenmiş anahtarları temizler; `portalOnay` `bulutId` için tekrar-onay kontrolü yapmaz. İkisi de onaylanırsa çift satış, çift stok düşümü, çift borç. Pencere dar ama iki cihazın sürekli açık olduğu mağazalarda gerçekçi. **Öneri:** `bekleyenPortal` id'sini `bulutId`'den türetin (deterministik) ya da `canliDomenUygula`'da `bulutId` ile tekilleştirin; `portalOnay/portalRed` başında `siparisIslendiMi(o.bulutId)` kontrolü.

### Y7. `bosBaslat` seq'i sıfırlayıp hesabı hammadde sayacıyla üretiyor → ilk yeni hesap aynı id'yi alıyor — `index.html:1281`, `4496`
`db.seq={}; db.hesaplar=[{id:nid('h'),…}]` → seq.h 1000→1001, hesap id 1000. Sonra `kaydetHesap` `nid('oh')` kullanır: seq.oh null → 1000 → **aynı id** (çok cihazda da aynı cihaz numarasıyla aynı). `hesap(id)` ilk eşleşeni döndürdüğünden yeni bankaya yapılan tüm işlemler Nakit Kasa'ya yazılır; senkronda `_recMerge` birini sessizce siler. `seqFix` ancak sonraki sayfa yüklemesinde onarır. Ayrıca `seq={}` maliyetler temizlenmediği hâlde `mk` sayacını sıfırlıyor → yeni maliyet kartı mevcut 1000 id'siyle çakışır. **Öneri:** Hesap için `nid('oh')`; seq'i sıfırlamak yerine yalnız temizlenen listelerin anahtarlarını sıfırlayın; sonunda `seqFix(db)`.

### Y8. `ayar` belgesi tüm-belge ezmeyle yazılıyor ve `seq` içinde olduğu için her kayıtta yeniden yazılıyor — `index.html:635`, `570`
`canliDomenUygula` yalnız `MERGE_LISTELER`, `seq`, `islenenSiparis`, `portalFav` için akıllı birleştirme yapıyor; kur, kademe, kampanya, firma*, duzenliGider, rolAyar, kuponlar, stokBildirim, malKat, portalTaslak `db[k]=d[k]` ile komple değişiyor ve `set()` ile komple yazılıyor. `seq` her `nid()` ile değiştiğinden herhangi bir cihazda herhangi bir kayıt oluşturmak ayar belgesinin tamamını o cihazın (bayat) değerleriyle yeniden yazar. Çevrimdışı kalan bir tablet bağlanınca kuyruktaki `set` patronun bu arada girdiği kampanya/kademe/kur değişikliklerini siler. **Öneri:** Skaler ayarları alan bazlı `_m` ile birleştirin ya da `set(...,{merge:true})` + yalnız değişen alan; `seq`'i ayrı belgeye alın; id'li listeleri (kuponlar, stokBildirim, duzenliGider, malKat, portalTaslak) `MERGE_LISTELER`'e ekleyin.

### Y9. `maliyetler`, `ortakHarcama`, `durakSablon` hiçbir `CANLI_DOMEN`'de değil; "Güvenli çıkış" bunları kalıcı siliyor — `index.html:563-571`
`ortakHarcama` girildiğinde hesap bakiyesi ve hareket senkronlanır ama kayıt diğer cihazlara gitmez (rapor cihaza göre farklı); `maliyetler` `MERGE_LISTELER`'de olmasına rağmen hiçbir domende yok. `guvenliCikis` "veriler ÖNCE buluta yüklenecek" deyip yalnız `CANLI_DOMEN` anahtarlarını yükleyip cihazı temizler → maliyet kartları, ortak harcamaları ve özel durak şablonları kaybolur (K2'deki IDB zombisi dışında). **Öneri:** `maliyetler`→`uretim`, `ortakHarcama`→`kasa`, `durakSablon`→`ayar`; `guvenliCikis`'te domen dışı anahtarları tespit edip uyarın.

### Y10. Senkron öncesi üretilen düz id'ler cihazlar arasında çakışıyor; "katıl" değiştirmek yerine birleştirip eziyor — `index.html:929`
`nid()` yalnız `canliAcik||db.canliBulut` iken cihaz numarası ekliyor; giriş yapmadan önce her cihaz 1000'den başlar. İkinci cihaz giriş öncesi kayıt girdiyse (deneme, ya da iki cihazın bir süre bağımsız kullanılması) aynı id'ler çakışır; taze `_m` damgalı yeni kayıt buluttaki gerçek kaydı ezer ve 687 ile yayılır; satış/çek referansları başka cariyi gösterir. **Öneri:** Hiç senkronlanmamış cihaz dolu buluta katılırken demo ise yereli temizleyin, değilse "değiştir / birleştir" sorun ve yerel kayıtlara yeni id verip referansları taşıyın; ya da id'yi baştan cihaz-ekli/UUID üretin.

### Y11. Yedekten dönüş kalıcı değil: IDB kopyası bir sonraki açılışta geri yüklenen veriyi eziyor — `index.html:880`, `7416`, `649`
`otoYedekGeri` ve `yedekYukle` yedeği localStorage'a yazıp `db=load()` yapıyor; `save()` çağrılmadığı için `db._t` eski kalıyor ve IDB güncellenmiyor. Sonraki açılışta IIFE `(+d._t) > (+mevcut._t)+1000` koşulunu sağlayıp geri yüklenen yedeği sessizce IDB kopyasıyla değiştiriyor. Kullanıcı restore sonrası hiçbir kayıt yapmadan kapattıysa dönüş tamamen kaybolur. **Öneri:** Her restore yolunda `db._t=Date.now(); save()` (en azından `idbYaz()`).

### Y12. İlk renkli teslimde malzemenin mevcut renksiz stoğu siliniyor — `index.html:1777-1794`
`malStoklaRenk` renk verildiğinde renk bulunamazsa yeni satır açıyor ve sonda `h.stok`'u **yalnız renkler toplamından** yeniden hesaplıyor. Malzeme daha önce renksiz tutuluyorsa (Aksesuar/Etiket/Ambalaj için form varsayılanı) sipariş teslimindeki ilk renkli satır eski toplam stoğu yok ediyor. Tüketim tarafı (`malStokUygula` ≈5598) `hamRenkli(h)` ile korunurken teslim tarafı korunmamış. **Örnek:** "Fermuar" 500 adet renksiz; "Siyah 18cm" 200 adet teslim → `h.stok=200`, 500 kayboldu. **Öneri:** `h.renkler` boş ve `h.stok>0` ise önce `{ad:'—',stok:h.stok}` satırı açıp eski toplamı taşıyın (parti taşıma mantığındaki gibi).

### Y13. Renkli malzemeye renksiz teslim `h.stok`'u renk toplamından koparıyor; otomatik aksesuar siparişinde stok eksiye düşüyor — `index.html:2157`, `4796`, `5598`
Renk ve parti boşsa `malStoklaRenk(h,'',inp)` yalnız `h.stok`'u artırır, renkler dokunulmaz → renkli malzemede "h.stok = renk toplamı" bozulur ve bir sonraki renkli işlem (1794) bu miktarı siler. `aksesuarAlimAc` otomatik siparişleri her zaman `satirlar:[{renk:''}]` ile açar; teslim tamamlanınca `aksesuarGeldiIsle → malStokUygula → malStoklaRenk(h,m.renk,-mik)` çalışır. **Örnek:** Düğme [{Siyah:0}], iş emri Siyah 500 alınacak → teslim 500 → `h.stok=500, Siyah=0` → tüketim → `Siyah=-500, h.stok=-500`. **Öneri:** Renkli malzemede renksiz girişi `'—'` rengine yazın; `aksesuarAlimAc`'ta satıra `m.renk`'i taşıyın.

### Y14. Fiş fotoğrafları base64 olarak `malSiparis`'e gömülüyor → `stok` belgesi 1 MB sınırını aşınca stok senkronu tamamen duruyor — `index.html:2162`, `2130`, `563`
Cloudinary ayarlı değilse (varsayılan) her teslimde 6'ya kadar fiş fotoğrafı (~200-400 KB) `s.fisler`'e ekleniyor; `malSiparis`, `urunler+hammadde+malKat` ile **tek** Firestore belgesine yazılıyor. 3-4 fişten sonra belge 1.048.576 baytı aşar, `set()` her save'de reddedilir ve sonsuza dek yeniden denenir; diğer cihazlar hiçbir stok değişikliğini görmez. `canliYaz` (733) yalnız `'longer than'` içeren hatada toast basıyor; belge boyutu hatası `'exceeds the maximum allowed size'` olduğundan kullanıcı uyarı görmez. **Öneri:** Fişleri daha agresif küçültün ve sipariş başına sınır koyun; Cloudinary yoksa fiş eklerken uyarın; `canliYaz`'da yazmadan önce `JSON.stringify` uzunluğunu kontrol edip `exceeds` hatasını da yakalayın; orta vadede fotoğrafları belgeden çıkarın.

### Y15. Satıştan doğan çek silinince/düzenlenince satışın `odenen`'i güncellenmiyor → müşteri borcu çift artıyor — `index.html:4784`, `4660-4663`, `3061-3084`
`applySatis` çekle ödenen satış için `cariIsle:true` çek üretir ama bakiyeyi `s.odenen` üzerinden düşer. `cekSil` `cariIsle` görünce `cc.bakiye+=c.tutar` yapar, satışın `odenen`/`_cekId` alanlarına dokunmaz; satış "ödendi" görünmeye devam eder, `satisKilitliCek` kilidi kalkar, `revertSatis` yalnız `(tutar-odenen)` kısmını geri alır → çek tutarı kadar borç kalıcı kalır. `kaydetCek` düzenlemede tutar değişince aynı sorun. **Örnek:** 10.000 ₺ satış çekle ödendi (borç 0) → çek silindi (borç 10.000) → satış silindi (`revertSatis` 0 geri alır) → hiç satış olmadan 10.000 ₺ borç. **Öneri:** `cekSil`/`kaydetCek` içinde `db.satislar.some(s=>(s.odemeler||[]).some(l=>l._cekId===id))` ise reddet ya da `l.tutar`, `s.odenen`, `_cekId`'yi birlikte güncelle.

### Y16. Tedarikçi/fasona "Çek" ile ödeme: bakiye düşülüyor ama çekte `cariIsle` yok → silme/düzenleme geri almıyor — `index.html:4380`
`kaydetOdeme` tedarikçi dalında `c.bakiye-=t` yapıp `cariIsle` ve `islemKur` olmadan verilen çek üretir; müşteri dalı (4377) bunları yazıyor. `cekSil`/`kaydetCek` yalnız `cariIsle` doğruysa bakiyeyi düzeltir. **Örnek:** Fasona 50.000 ₺ çek (borç düşer) → çek silinir → borç geri gelmez, hareket kaydı kalır. **Öneri:** 4380'deki objeye `cariIsle:true,islemKur:+db.kurUSD||1` ekleyin; çeke `hareketId` yazıp silmede hareketi de kaldırın.

### Y17. Üretilen renkler üründe yoksa mamul yanlış renklere oransal dağıtılıyor — `index.html:5539-5541`, `5575-5577`
`isEmriTamamlaGoster` satır listesini mevcut ürün varsa **yalnız ürünün renklerinden** kuruyor; son durağın gerçek renk sayımı (`sonRenk`) birebir ad eşleşmesi arıyor, eşleşmeyince `oran=1/cols.length` ile sağlam adet mevcut renklere bölünüyor. `isEmriStokla` yalnız bu satırları okuduğu için üretimde çıkan renk stoğa hiç yazılmıyor; "eşleşmeyen renk için yeni satır" dalı mevcut üründe tetiklenmiyor. **Örnek:** Ürün renkleri Siyah/Beyaz; kumaştan seçilen iş emri rengi "Lacivert Melanj" 100 adet → Stoğa Al ekranı Siyah 50 / Beyaz 50 önerir. Büyük/küçük harf farkı da aynı sonucu verir. **Öneri:** `cols` = ürün renkleri ∪ `sonRenk` anahtarları (harf duyarsız eşleştir, eşleşmeyeni ayrı satır olarak ekle); prefill'i oransal değil gerçek `sonRenk` ile yap.

### Y18. Renkli kumaşta "renk seçmeden toplamdan düş" seçeneği bir sonraki renkli hareketle siliniyor; düzenlemede iade stoğu şişiriyor — `index.html:5598-5605`, `5283`
`malStokUygula` renk boşsa yalnız `h.stok+=mik` yapıyor, `renkler[]` dokunulmuyor; sonraki herhangi bir renkli hareket (`malStoklaRenk` 1797: `h.stok=sum(renkler)`) düşümü siliyor. Ardından `ieMalKaydet` aynı malzemeyi `+1` ile iade ederken hiç düşülmemiş miktar geri ekleniyor. Y12/Y13 ile aynı kök: "h.stok = renk toplamı" invariantı renksiz yol tarafından bozuluyor. **Öneri:** `hamRenkli(h)&&!m.renk` iken kaydı reddedin (renk zorunlu) ya da düşümü `'—'` rengine yazın.

### Y19. Ürün detayındaki "Satışa Ekle" ile açılan satışta renkli ürünün stoğu hiç düşmüyor — `index.html:2836`, `3048-3055`, `1313`
`formSatis(pre)` dalı kaleme `renkler` koymadan `{uid,mik:1,…}` ekliyor. `stokUygula` renkli üründe `k.renkler` yoksa `u.stok-=k.mik` yapıyor; ama renk stoklu üründe `u.stok` türetilmiş alan, ilk `syncStok(u)` renk toplamından yeniden hesaplayıp düşümü siliyor. Yeterlilik kontrolü `stokTop(u)` ile geçtiği için uyarı da yok. **Öneri:** `pre` dalında renkli üründe sepete eklemek yerine renk seçim panelini açın; `kaydetSatis`'te `hasRenkStok(u)&&!k.renkler` kalemleri reddedin.

### Y20. Satış düzenlerken "+ yeni müşteri ekle" satışı kopyalıyor — `index.html:2911-2921`
`satisMusteriEkle` stash'e `sEdit`'i koymuyor; `saleRestore` `formSatis()`'i parametresiz çağırıyor → `sEdit=0`. Kullanıcı artık "Satışı Tamamla" görür; `kaydetSatis`'te `old=null` olduğu için `revertSatis` çalışmaz, yeni satış açılır; eski satış stok/bakiye etkisiyle kalır (çift stok, çift borç). `sVade`, `sFoto`, `sDepo` da stash'lenmediği için kaybolur. **Öneri:** stash'e `sEdit,sVade,sFoto,sDepo` ekleyin; `saleRestore` içinde `formSatis(0,st.sEdit)`.

### Y21. Düzenlemede vade farkı sıfırlanıyor → hiçbir şey değiştirmeden kaydetmek borcu %2,5-7,5 azaltıyor — `index.html:2833-2837`, `2950-2959`, `3110-3113`
Düzenleme dalı `eIsk,eKdv,…` yüklüyor ama `sVade`'yi `s.vadeFarki`'den geri kurmuyor (`sVade=0`). `satisHesap` `vf=0` ile tutarı hesaplar; `commit` eski satışı `revertSatis` ile geri alıp yeni düşük tutarla `applySatis` yapar; `data.vadeFarki=0` yazılır. **Örnek:** 90 gün vadeli (+%7,5) 100.000 ₺ satışta sadece sevkiyat notu değiştirilir → toplam 93.023 ₺, cari borç 7.500 ₺ azalır. **Öneri:** düzenleme dalında `sVade`'yi `vadeFarki`'den (ya da yeni `vadeGun` alanından) geri kurun ve `setVade(sVade)` çağırın.

### Y22. Taksiti tahsil edilmiş satış iptal edilince kasa geri alınmıyor, müşteriye alacak yazılmıyor — `index.html:3072-3087`, `3176-3181`, `4076`
`taksitOdeKaydet` `h.bakiye+=paidTL; m.bakiye-=paidTL; s.odenen+=t.tutar` yapıyor ve `s.odemeler`'e satır eklemiyor. `revertSatis` bakiyeden `(tutar-odenen-kesik)*div` düşüyor ve yalnız `s.odemeler` içindeki `_hesapId/_cekId` satırlarını kasadan geri alıyor; taksit için dal yok. **Örnek:** 30.000 ₺ satış, 1. taksit 10.000 ₺ tahsil → "Satışı İptal Et" → kasa +10.000 kalır, müşterinin ödediği para kayıtlarda yok olur; `tahsilat` hareketi yetim kalır. **Öneri:** `revertSatis`'te `(s.taksitler||[]).filter(t=>t.odendi)` için alacak yazın ya da kasadan iade edin; ilgili hareketleri `satisId` ile işaretleyip silin; taksitli satış iptalinde uyarı.

### Y23. Portal sipariş alanları tip kontrolsüz: yönetim ekranı çöküyor, fiş/faturaya XSS taşınıyor, "Fiyat Belirle" penceresinde ham `value` — `index.html:5789-5791`, `5857`, `5876-5877`, `3130`, `3472`
`mus:o.mus`, `kalemler:o.kalemler||[]`, `tutar:o.tutar||0` tip kontrolü olmadan alınıyor. (a) `o.mus` sayı/obje ya da `kalemler` string ise `rPortal` TypeError verir, `$('app').innerHTML` hiç yazılmaz → yönetici Portal sekmesine giremez, kaydı reddedemez. (b) `portalOnay` `mik:k.mik` ham kopyalıyor; `satisFis` (3130) `${k.mik} ${k.birim}` ve `faturaYazdir` (3472) kaçışsız basıyor → onaydan sonra fiş açılınca çalışan XSS. (c) `portalFiyatBelirle` `value="${o.tutar}"` ve `onclick="portalOnay(${id},…||${o.tutar})"` üretiyor; fiyatı 0 olan kalem varsa bu pencere onay tuşuyla otomatik açılıyor (5841). **Öneri:** Tek bir `siparisNormalize(o)` fonksiyonu: `String()`/`Number()`/`Array.isArray` zorlaması; `rPortal`'da kart başına try/catch + bozuk kayıt için Reddet; 3130/3472'de `esc(k.birim)`.

### Y24. Davet linkindeki `c=<cariId>` müşteri tarafından seçilebiliyor; onay ekranı bu cariyi önseçiyor — `index.html:6222`, `1058`, `1136-1139`, `1002`
`portalDavetOku` hash'ten `c=(\d+)` okuyor, `bulutKayit` bunu `istenenCari` olarak `kullanicilar` belgesine yazıyor (kurallar kısıtlamıyor). Yönetici ekranı "✉️ Davetle geldi — önerilen cari: X" gösterip `<select>`'i önseçiyor; tek tuş onayda `musteri_veri/{uid}` o carinin bakiye/limit/geçmişini bu hesaba yayınlıyor. Cari id'leri ardışık (1000+). **Öneri:** `istenenCari`'yi yalnız yöneticinin ürettiği tek kullanımlık davet token'ı ile kabul edin; önseçim yapmayın.

### Y25. Stok raporu USD'li ürünlerin alış/satış değerini TL'ymiş gibi topluyor — `index.html:7175-7185`, `2963`
Ürünlerde `pb` var ve diğer yerler `urunAlisTL(u)` kullanıyor; `rapStok` Ölü/Yavaş Stok'ta `u.stok*(+u.alis||0)`, Toplam Stok Değeri'nde `Σ u.stok*u.alis` / `Σ u.stok*u.satis` ile ham değer topluyor. 100 adet, alış 10 $ (kur 40) → "Maliyet değeri ₺1.000" (doğrusu ₺40.000). Özet sekmesindeki Ölü Stok (7040) doğru `urunAlisTL` kullandığı için iki sekme aynı ürüne farklı tutar gösterir. **Öneri:** `urunAlisTL(u)` / `urunSatisTL(u)/birimAdet(u)`.

### Y26. Yazıcı ajanında `hamBaski:false` ayarı metin modunda tüm fişleri "hata"ya düşürüyor — `yazici-agent/agent.js:118-166`, `index.html:3893-3897`
Uygulama metin modunda (`db.yaziciMetin!==false`, varsayılan) `resim:''` gönderiyor → ajanda `imgs=null`; `escpos` olduğu için erken `return` geçiliyor, ama `useRaw=escpos && cfg.hamBaski!==false` yanlışsa `else` dalında `imgs.length` okunuyor → TypeError → `durum:'hata'`. `config.example.json`'daki `_not_hamBaski` tam bu ayarı öneriyor. **Öneri:** Ajanda `if(!useRaw && (!imgs||!imgs.length))` için açıklayıcı hata; ya da uygulama `hamBaski:false` için resmi de göndersin.

---

## 3. ORTA

Raporlar
- `index.html:7143-7144` — Gider sekmesi "NET KÂR" kartı `brütKâr − gToplam` yazıyor ama `net=R.netKar` yalnız işletme giderini düşüyor; fason/tedarik ödemesi olan dönemde satır ile sonuç uyuşmuyor.
- `index.html:6856-6875`, `6984-6986`, `7060` — "Bu Ay/Bu Yıl" ay-içi (bugüne kadar) veri, önceki dönem ve geçen yıl tam takvim dönemi ile kıyaslanıyor; ayın 3'ünde "Geçen yıla göre %90 düşüş".
- `index.html:6975-6985` — Dönem karşılaştırmada "Tahsilat" iki farklı tanımı kıyaslıyor (`tahsil` peşin−taksit+hareketler; `tahOnc` yalnız `Σ odenen`).
- `index.html:7027-7029`, `7196` — Ürün/Müşteri kârlılığı iskonto ve vade farkını yok sayıyor (liste fiyatı × adet); Gelir Tablosu'ndaki brüt kârdan sistematik yüksek çıkar.
- `index.html:7071`, `4240` — Ödeme dağılımı ve ödeme geçmişi bugünkü kurla, geri kalan rapor satış kuruyla çevriliyor; aynı 1.000 $ ödeme iki kartta farklı TL.

PWA / Termal
- `index.html:7481-7486` — `controllerchange` global: başka sekme "Yenile" deyince bu sekme de yenileniyor; koruma yalnız `sepet.length`'e bakıyor, açık modal formlar (iş emri, tahsilat) kaybolur.
- `index.html:3776-3778` — Termal özel karakter haritasında büyük/küçük eş türetme Ç/Ş/Ğ'yi küçük harfin baytına bağlıyor (`m[u]=n-0x20>=0x80?n-0x20:n` → `Ç`=0x87); `m[l]=m[l]` satırı boş işlem. Ekrandaki örnek aynen yazılınca "ÇANTA" → "çANTA".

Portal
- `index.html:6027-6032`, `6207`, `6212` — Yerel portal girişi: şifre düz metin (`c.sifre`), `cari` senkronuyla mağaza rolüne kadar okunuyor; 5 deneme kilidi yalnız bellekte; oturum `localStorage['oz_portal_oturum']=cariId` (id yazan herkes o müşteri olur). Bulut kimliğine geçin ya da en azından özet + rastgele oturum token'ı.
- `index.html:5844` — Kataloğa eşleşmeyen `uid` içeren kalem `if(!u)return` ile atlanıyor; tüm kalemler eşleşmezse `esle>0` sağlanmaz ve müşteri fiyatı doğrudan satışa geçer, stok düşmez.
- `index.html:6343`, `6329` — `pazarOlay` her sayfa yüklemesinde yeni belge; bulutta temizleme yok, kurallar `magaza` alanını doğrulamıyor → girişli herhangi bir hesap sınırsız belge yazabilir. Oturum id'sini `uid+gün` yapın, 90 gün bakım silmesi ekleyin.
- `index.html:1423`, `1426`, `1523`, `1533` — PIN özeti 32-bit djb2; yedek JSON'u ele geçiren kişi 4-8 haneli PIN'i anında kırar. `crypto.subtle` (PBKDF2/SHA-256+salt) kullanın.

Satış / Fiş
- `index.html:3096-3113`, `3060-3066` — Düzenleme, geçmiş dövizli ödemeleri ve taksitleri **bugünkü** kurla yeniden değerliyor (`div=h.div`, `data.satisKur=div` eskiyi ezer): kur 32→40 iken sadece notu düzenlemek kasaya +800 ₺ hayali para yazar. Düzenlemede `div=old.satisKur` kullanın ya da ödeme satırına `kur` yazın.
- `index.html:2967-2979` — USD üründe fiyat kutusu "$" etiketiyle TL değer gösteriyor; `fiyatBilgi` `u.alis` (USD) ile TL `fy`'yi çıkarıp "kâr $3180 (%5300)" basıyor (`urunAlisTL` kullanılmamış).
- `index.html:3088`, `3072-3078` — Satışın çeki Çekler ekranından silinirse `satisKilitliCek` `false` döner; satış iptalinde `_cekId` bulunamayınca bakiyeye dokunulmaz → müşteri bakiyesi çek tutarı kadar fazla kalır (Y15 ile aynı kök).
- `index.html:3493-3495`, `2686` — `faturaYazdir` ve `barkodYap` `window.print()` döner dönmez `printing` sınıfını kaldırıyor; iOS'ta önizleme boş/yanlış çıkabilir. `fisTermal` (3527-3536) `afterprint` + zamanlayıcı ile düzeltilmiş, bu ikisi almamış.

Üretim / Maliyet
- `index.html:5580`, `2963` — İş emri birim maliyeti her zaman TL üretiliyor ama USD `pb`'li ürünün `alis` alanına doğrudan yazılıyor; `urunAlisTL` bunu tekrar kurla çarpar (₺420 → $420 ≈ ₺13.600 maliyet). Kutu varsayılan işaretli (5551).
- `index.html:5345-5349` — Kumaş tüketimi iş emri açılışında hemen düşülüyor ama iş emri silme/iptal fonksiyonu yok (`isemirleri` üzerinde yalnız `push`); `ieMalDuzenle` `kumasId/kumasTuk*` alanlarını ele almıyor. Yanlış gramaj (230 yerine 2300) kalıcı stok kaybı.
- `index.html:5176`, `5190`, `5347` — Kumaş tüketimi üç modda da kg/parça üretiyor; hammadde `birim` metre/top olsa da `stok`'tan aynen düşülüyor ("230 gr → 8,5 metre").
- `index.html:5151`, `5341-5345` — "Elle gir" moduna geçince `ieKumasId/Ad` sıfırlanıyor ama `#ie-kumas-tuk`, `#ie-kumas-fiyat` ve `ieKumasFiyat` duruyor → `kumasAd:''` olan hayalet kumaş maliyeti kaydediliyor.

Kasa / Çek / Cari
- `index.html:4372` — Negatif tahsilat tutarı `kesik>0` iken kabul ediliyor (`if(t<=0&&kesik<=0)`); borç artar, kasa azalır, ekstrede "Tahsilat −500". `t<0||kesik<0` reddedilmeli.
- `index.html:4780`, `4787` — Karşılıksız/silinen çekte bakiye geri alınıyor ama `tahsilat` hareketi kalıyor; ekstre devri kayar, `sonTahsilat/gecikmeGun` müşteriyi "ödedi" sayar, rapor tahsilata ekler. Çeke `hareketId` yazıp ters kayıt atın.
- `index.html:4772`, `4668`, `4759` — Ciro, çek düzenleme ve kur farkı bakiyeyi doğrudan değiştiriyor; `hareketler`'e yazmıyor, `cariEkstre` `db.cekler`'i okumuyor; yanlış tedarikçiye ciro geri alınamıyor ("Ciroyu geri al" yok).
- `index.html:4721` — Çek işlem penceresinde `${c.cari}` kaçışsız (`esc` atlanmış); 4672'de `fotoArka` için `guvenliSrc` yok.

Veri / senkron
- `index.html:619` — 30 günlük mezar taşı budaması, bulutla `_tombMerge` (union) yapıldığı için geri geliyor; her save'de fazladan yazım, mezar taşları asla silinmiyor.
- `index.html:612` — `_m` ve mezar taşı damgaları cihaz saatinden; saati ileri olan cihaz birleştirmeyi tek yönlü kazanır. Sunucu zamanı (`serverTimestamp`) ya da açılışta saat farkı düzeltmesi düşünün.
- `index.html:928` — `CIHAZ_NO` 1-99 arası koordinasyonsuz rastgele; iki cihaz aynı numarayı alırsa id'ler çakışır.
- `index.html:721` — `canliGonder`/`canliYazHemen` hatada `_canliGolge`'yi temizlemiyor; başarısız domen veri değişene kadar bir daha denenmiyor.
- `index.html:746` — Domen belgeleri sınırsız büyüyor (hareketler/satislar/islenenSiparis); arşiv yok; 1 MB'de senkron kalıcı kırılır. `islenenSiparis` budaması union ile geri gelir.
- `index.html:672` — `autoYedek` her açılışta ("senkron öncesi") çalışıp 5'lik FIFO'yu dolduruyor; kritik yedekler 5 açılışta silinir; arayüz "son 10" diyor.
- `index.html:880` — Senkron açıkken yedeğe dönüş bir sonraki `otoSenkron`'da bulut tarafından geri alınıyor (kullanıcıya söylenmiyor).
- `index.html:1010` — `gercekVeriVar()`'daki "girişli personel" koşulu tek çağrı noktasında sağlanamıyor; cari/satış görmeyen personel cihazında katalog yerel ürün verisini ezebiliyor.

Bulut / yetki / kurallar
- `index.html:1138` — Onay ekranı kullanıcının kendi yazdığı `istenenRol` ve `istenenCari` değerlerini varsayılan seçili getiriyor; tek tıkla yanlış rol/cari verilebilir. Varsayılanı "musteri / cari yok" yapıp isteği ayrı gösterin.
- `index.html:1134` — `bulutKullaniciYonet` rol listesinde `fasontakip` yok; istenen rol fasontakip olan kullanıcı sessizce `musteri` olarak onaylanır.
- `index.html:1037` — Bulut siparişlerinde `kullanicilar.cariId` bağı hiç kullanılmıyor; sipariş ada göre eşleşiyor ya da mükerrer cari açılıyor (Y4 ile aynı kökten).
- `index.html:1256` — `siparisDinle` izin hatasında ölen dinleyici `_sipUnsub`'ı dolu bırakıyor; bir daha başlatılamıyor, ekran "✓ dinleniyor" gösteriyor.
- `index.html:974` — Herkese açık `magaza` belgesine `fiyatGizli` ürünlerin gerçek `satis` fiyatı ve tüm aktif kupon kodları (limit/kullanım dahil) yazılıyor. Gizli fiyatı 0 gönderin; kuponu doğrulama için ayrı, okunamayan bir yapı ya da yalnız kod hash'i.
- `index.html:974` — Katalog belgesine base64 fotoğraflar (`renkler[].foto`) olduğu gibi yazılıyor; Cloudinary yoksa 1 MiB aşılır ve otomatik yayın sessizce ölür.
- `index.html:1188` — `bulutFasonYayinla` silinen/değişen durakların `fason_isemri` belgelerini pasifleştirmiyor; fason hayalet işleri aktif görür.
- `index.html:1052` — Kayıtta e-posta verilirse kimlik e-posta olur; arayüzün vaat ettiği "telefonla giriş" çalışmaz.
- `index.html:6044` — Portal kullanıcı şifresi `cari.sifre` içinde **düz metin** saklanıyor ve `cari` domeniyle buluta gidiyor (admin/ortak/mağaza okur). Firebase Auth zaten var; bu alanı kaldırın ya da yalnız tek kullanımlık davet kodu üretin.
- `firestore.rules:78` — Fason kendi `fason_isemri` belgesinde her alanı (adet, ücret, aktif, fasonUid) değiştirebiliyor; kod yalnız durum/teslimAdet güncelliyor. `request.resource.data.diff(resource.data).affectedKeys().hasOnly(['durum','teslimAdet',...])` ekleyin.
- `firestore.rules:56` — `siparisler` create'te alan/tip doğrulaması yok (Y4/Y5'in kural tarafı).

Stok / envanter
- `index.html:2165` — "Siparişi kapat" ile eksik teslimde iş emri malzemesinin tamamı stoktan düşülüyor → eksi stok.
- `index.html:2008` — Sipariş birimi malzeme kartının birimiyle eşleşmiyor; teslimde dönüşümsüz toplanıyor (metre sayısı kg stoğuna).
- `index.html:2172` — Tedarikçi carisi yoksa teslimde borç sessizce yazılmıyor; ekran "borç yazılır" diyor.
- `index.html:1792` — Eksiye düşen renk stoğuna yeni parti gelince parti toplamı ≠ renk stoğu; malzeme kartı kaydedilince parti kodları `'—'`ya çöküyor.
- `index.html:1789` — Ton farkı uyarısı toast'ı hemen sonraki toast tarafından eziliyor, hiç görünmüyor.
- `index.html:2130` — Fiş yüklemesi bitmeden "Teslimatı Stoğa Ekle" basılırsa fişler sessizce kayboluyor.
- `index.html:2316` — Cloudinary yüklemesi başarısız olunca sessizce base64'e düşülüyor; bildirim/yeniden deneme yok (Y14'ü besliyor).
- `index.html:1730` — Stok listesi parça fiyatını `/seri` etiketiyle gösteriyor (seri ürünlerde fiyat 5 kat düşük görünüyor).
- `index.html:1760` — Envanter, kâr yetkisi olmayan rollere (fason/fasontakip) malzeme alış fiyatlarını gösteriyor.
- `index.html:1748` — Envanter'de seçili kategori (`envMalKat`) kaçışsız basılıyor → senkronla gelen kategori adıyla saklı XSS.
- `index.html:1553` — "Tutarlar gizli" modu Bekleyen İşler listesinde ve bazı ekranlarda PIN'siz tutar gösteriyor.

Ajanlar / dağıtım
- `sessiz-yazici/IQ-Basics-Sessiz-Yazici.bat` — Dosya **LF** satır sonlu (CR satırı 0/34). Deponun kendi commit mesajı ("bat dosyaları CRLF yapıldı — asıl hata buydu") ve `.gitattributes` bunun cmd.exe'de "'d' is not recognized" hatasına yol açtığını söylüyor; bu dosya o düzeltmeden nasibini almamış. Diğer 13 bat/vbs/ps1 dosyası doğru (CRLF).
- `wa-agent/ornek-dinleyici.js:66-93` — `docChanges().forEach(async …)` tüm bekleyen mesajları **aynı anda** gönderiyor; hiçbir bekleme/oran sınırı yok. 30 hatırlatma mesajı toplu kuyruğa atılınca WhatsApp hesabı için ban riski. Sıralı işleme + gönderimler arası 3-8 sn rastgele bekleme ekleyin; `client.getNumberId()` ile numara kayıtlı mı kontrol edin.
- `wa-agent/ornek-dinleyici.js:82-85` — Numara normalizasyonu: 12 haneli ve `90` ile başlamayan yabancı numaralar (`+49…`) `no.length<11` kontrolünü geçip `@c.us` ile gönderilir; sorun değil ama `0090…` biçimi `009…` olarak kalır ve reddedilmez.

## 4. DÜŞÜK

Raporlar / PWA / Termal
- `index.html:7103-7106` — "Satışların %X'i tahsil edildi" %100'ü aşabiliyor (pay: dönem içi tüm tahsilatlar; payda: dönem satışları).
- `index.html:7239`, `6937` — Sezon sekmesi ve ciro grafiği KDV dahil brüt, kartlar KDV hariç net; aynı ekranda iki "ciro".
- `index.html:7448-7452`, `sw.js:73` — "Uygulamayı Güncelle" tüm cache'i siliyor; firebase CDN scriptleri `crossorigin`'siz (opaque, status 0) olduğu için runtime handler onları bir daha kaydetmiyor → sonraki çevrimdışı açılışta `window.firebase` yok.
- `index.html:644-655`, `7440-7442` — IDB kurtarma IIFE'si asenkron: localStorage bozuksa bulut dinleyicileri kurulduktan sonra `db=d` ile birleştirilmiş veriyi ezebilir; `autoYedek` kurtarmadan önce çalışıp boş veriyi yedekleyebilir.
- `index.html:3798-3806` — Fiş fiyat/toplam sütunları `cutx` ile kesiliyor: ₺125.000 → "125.000,0."; toplam ≥ ₺10.000.000 bozuluyor.
- `index.html:3703-3704`, `3720` — Raster modda `ESC 3 0` sonrası `ESC d 3` fiilen kağıt ilerletmiyor (satır aralığı 0); kesim konumuna otomatik beslemeyen yazıcılarda QR'ın altı kesiliyor.
- `index.html:3749-3755` — `cp1254_16` profili: `ESC t 16` çoğu yazıcıda WPC1252'dir; etiket yanıltıcı.
- `index.html:4256` — `waTel` "00" önekli uluslararası numaraya da "9" ekliyor ("0049…" → "90049…").

Portal
- `index.html:6816-6824`, `6757-6768`, `6694`, `6728`, `6430-6436` — `PT_SOZ` sözlüğündeki 101 anahtarın tamamı kullanılıyor ama şablonda `L()`'den geçmeyen çok sayıda sabit Türkçe metin var ("Kayıtlı sepetin var", "Son siparişini tekrarla", filtre başlıkları, "tükendi/az kaldı", "Hesabınız onay bekliyor", toast'lar); `open_` modalları `pov` dışında olduğu için RTL `dir` almıyor.
- `index.html:4672` — Çek arka yüz fotoğrafı `guvenliSrc` olmadan `<img src>`'ye basılıyor (ön yüzde var).

Satış / Fiş
- `index.html:3164-3167` — Taksit bölme: `her=floor(kalan/n)`, son taksit `Math.round(kalan-top)` → kesirli kalanda toplam ±0,5 PB sapar; tüm taksitler ödenince satış "ödendi" olmaz ya da sahte alacak doğar.
- `index.html:3120`, `3299`, `3452`, `3162` — `satisFis`, `waFisPreview`, `fisWhatsapp`, `odemePlaniKaydet` silinmiş (başka cihazdan mezar taşıyla giden) satış id'sinde null kontrolsüz çöküyor; kullanıcı geri bildirim almıyor.
- `index.html:2853`, `2833` — Düzenlemede para birimi segmenti her zaman ₺ "on" görünüyor (`setPB(sPB)` çağrılmıyor); USD satışta ₺'ye "geçmek" aslında çevirim yapar.
- `index.html:2918-2921` — `saleRestore` sonrası `musBilgi()` elle girilen iskontoyu grup iskontosuyla eziyor.
- `index.html:2481-2500` — `barkodTara` ikinci kez çağrılınca önceki `MediaStream` durdurulmuyor; kamera açık kalır, iOS'ta ikinci `getUserMedia` reddedilebilir.

Üretim
- `index.html:4817` — `aksesuarGeldiIsle` eşleşmede `s.hid&&m.hid===s.hid` fallback'i kullanıyor; aynı hammaddeden iki "alınacak" satır varsa ilk teslimat ikisini de `var` yapıp erken düşüyor (geçici eksi stok). Yalnız `alimId` ile eşleştirin.
- `index.html:5299` — "Stoğa tanımla" her malzemeyi `kat:'Kumaş'` yapıyor; alış fiyatında yalnız USD kuru var, EUR TL sayılıyor.
- `index.html:5124` — 4. adım metni kumaşın malzeme listesine de "elimde var" olarak girilmesine yol açıyor; `ieMalKaydet` `m.hid===i.kumasId` kontrolü yapmadığı için çift düşüm + çift maliyet mümkün.

Kasa / Çek
- `index.html:4410-4416` — Kasa "Bugün Giren/Çıkan" ve "Bu Ay Nakit Akışı" iç transferleri (`tur:'Transfer'`, in+out) iki yönde de sayıyor; 100.000 ₺ kasa→banka aktarımı "+100.000 / −100.000" görünür.
- `index.html:4377` — Çekli tahsilatta seçilen hesap yok sayılıyor (`hesapId` yazılmıyor; KK dalında yazılıyor); `cekTahsil` nakit kasaya düşer.
- `index.html:4633`, `4647` — `cekCariYeni` hemen cari yaratıp kaydediyor; ad sonradan değişirse ikinci cari doğar; karşılaştırma `toLowerCase` ile (Türkçe İ/ı eşleşmez) ve `x.ad` boşsa fırlatır.
- `index.html:4514` — Düzenli gider hareketi silinince `duzenliGider.sonAy` sıfırlanmıyor; o ay bir daha "işle" yapılamıyor.
- `index.html:4490`, `4895`, `7121` — USD çalışan fason/tedarikçi borcu (`bakiyeUSD`) nakit takviminde, fason karnesinde ve raporda 0 görünüyor.

Genel
- `index.html:968` — `bulutGirisYap`: sahip e-postası için yanlış "yönetici değil" mesajı; 969'da ölü dal.
- `index.html:996` — `db.yayinSon` `save()` olmadan atanıyor; yenilemede kaybolur.
- `index.html:1108` — `bulutTumKullanicilar` hataları yutuyor; izin/ağ hatasında boş liste / "Fasona bağlı iş yok".
- `index.html:634` — `portalFav` union birleştirildiği için favoriden çıkarma senkron açıkken geri alınıyor.
- `index.html:696` — `canliDur()` içindeki `localStorage.setItem` try/catch'siz; kota doluysa fırlatır ve çağıran akış yarım kalır.
- `index.html:651` — IDB'den geri yükleme `load()`'daki normalizasyon/`seqFix` adımlarını atlıyor.
- `index.html:1344` — 30 gün ciro trendi iadeleri düşmüyor; aynı panelde "Bugünkü Satış" kutusuyla çelişiyor.
- `index.html:1304` — `gunFark` `Math.round` kullanıyor; "BUGÜN"/"GECİKTİ 1g" yarım gün kayıyor.
- `index.html:1634` — "En Çok Satan" satırı silinmiş/uid'siz ürün için `urunDetay`'ı çağırıp çöküyor.
- `index.html:1439` — Arka plandan dönünce hareketsizlik sayacı sıfırlanıyor; süre dolmuşsa bile kilit devreye girmeyebiliyor.
- `index.html:1777` — Renk/parti anahtarı Türkçe için normalize edilmiyor ('KIRMIZI' ≠ 'Kırmızı' — `toLowerCase` 'I'→'i' verir).
- `index.html:2150` — `kaydetMalSipTeslim` doğrulamadan önce veri değiştiriyor (hayalet hammadde kartı, alış fiyatı ezilmesi).
- `index.html:1951` — İş emri resminde `x.roundRect` korumasız; desteklemeyen tarayıcıda önizleme düşüyor.
- `index.html:2111` — `open_` başlığı `textContent` ile yazıldığı için `esc()` çift kaçış üretiyor ('&' → '&amp;').
- `index.html:2444` — Kritik stok 0 kaydedilemiyor: `||10` sıfırı 10'a çeviriyor.
- `index.html:2295` — `kaydetSayim`: input bulunamayan renk satırının stoğu sessizce 0'a çekiliyor.
- `index.html:2196` — Kısmi teslim alınmış sipariş iptal edilince gelen miktar/borç bilgisi görünmez oluyor.
- `README.md` — Hâlâ Kodluyoruz eğitim şablonu ("bir adet index.html bulunuyor", MIT linki google.com). Projeyi anlatan bir README yok.
- `YAYINLAMA.md:83-84` — "Admin yalnız Firestore'dan elle atanır" ifadesi güncel değil: `firestore.rules` sahip e-postasına kendi belgesini admin olarak oluşturma izni veriyor ve uygulamada "Bu Hesabı Yönetici Yap" var.

## 5. Ayrıca incelendi, sorun bulunmadı

- **Yinelenen tanım yok:** 811 top-level fonksiyon ve top-level const/let/var arasında aynı ada sahip ikinci tanım yok (sessiz ezme riski yok).
- **Sürüm tutarlı:** `APP_SURUM='2026.07.24-101'` ile `sw.js` `CACHE='iqbasics-v101'` aynı numarayı taşıyor. SW güncelleme akışı (SKIP_WAITING → controllerchange, satış sürerken yenileme yok) mantıklı.
- **Yazdırma kuyruğu alan uyumu:** uygulama ↔ ajan alan adları (`durum:'bekliyor'`, `olusturma`, `escpos` ham base64, `resim` data-URL, `kopya` 1-5, `_ajan_durum`) eşleşiyor; GS v 0 raster bayt hesabı ve QR paket uzunlukları doğru; 576 px + 950.000 bayt kontrolü Firestore sınırı için yeterli. (Tek istisna Y26.)
- **yazici-agent/agent.js:** Yazıcı adı yalnız yerel `config.json`'dan geliyor (buluttan gelen alan PowerShell argümanına girmiyor → komut enjeksiyonu yok); iş sahiplenme `runTransaction` ile; takılı iş kurtarma, 6 saat süre, 8 MB boyut sınırı, seri işleme, dinleyici hatasında çıkış + bat/vbs yeniden başlatma doğru. PS1 dosyaları BOM'lu ve CRLF. Tek not: `_ajan_durum` belgesi `yazdirma` koleksiyonunda `durum:'ajan'` ile tutuluyor; kuralda `yazdirma` için mağaza da `create` yapabildiğinden bir mağaza hesabı sahte "ajan bağlı" belgesi yazabilir (yalnız gösterge etkiler).
- **Testler:** 6 dosya gerçek akışları sürüyor, `pageerror` sonucu etkiliyor, çıkış kodları doğru. Eksik: senkron birleştirme (`_recMerge`/`canliDomenUygula`), çek yaşam döngüsü, üretim stok düşümü, rol izinleri ve Firestore kuralları için test yok; CI yok.

## 6. Öneriler (öncelik sırasıyla)

1. **Cihaz temizleme/sıfırlama/çıkış akışlarını tek fonksiyonda toplayın** (`cihazSifirla({demo, yedegiKoru})`): IndexedDB'yi sil, `_tomb`'u temizle, `_mSnapInit()` çağır, `db.canliBulut=false` bayrağına `otoSenkron` ve açılış kodu saygı göstersin. K1, K2, K3'ün büyük kısmını tek seferde kapatır. (efor: küçük, etki: yüksek)
2. **Demo veriyi işaretleyin ve asla buluta yazmayın:** seed kayıtlarına `_demo:true`; `canliBaslat` öncesi `db.ornek` ise listeleri boşaltın; dolu buluta katılan hiç senkronlanmamış cihaza "Bu cihazdaki veriyi bulutla DEĞİŞTİR / BİRLEŞTİR" sorun. (küçük–orta, yüksek)
3. **Yönetici kapısını `paraGoster()`'dan ayırın:** `adminMi()` tanımlayın; rol/izin/sıfırlama/yedek/çıkış/toplu zam işlemleri yalnız bununla açılsın; `rolIzinSet` ve `setRol` içinde kontrol; `rolAyar`'ı admin-only domene taşıyın. (küçük, yüksek)
4. **Portaldan gelen belgeleri şemayla temizleyin ve kurallarda doğrulayın:** tek bir `siparisNormalize(o)` (String/Number/Array zorlaması) hem `siparisDinle` hem `bulutSiparisleriCek` girişinde; kuponu onayda `kuponBul` ile yeniden hesaplayın, `tutar<=0` reddedin; `siparisler` create'te `kalemler` tipleri, `tutar` sayı, `mid` yok (cariId `kullanicilar`'dan bağlanır); `pazarOlay` alanlarını sayıya zorlayın + boyut sınırı; `fason_isemri` update'te `affectedKeys().hasOnly([...])`. Kural birim testleri için `@firebase/rules-unit-testing`. K6, K7, Y4, Y5, Y23'ü birlikte kapatır. (orta, yüksek)
5. **Bakiye ve stoku defterden türetin ya da çakışmayı yakalayın:** `cari.bakiye`/`hesap.bakiye`'yi hareket+satış+ödeme kayıtlarından hesaplayan tek fonksiyon (defter kontrolü zaten var); birleştirmede iki tarafta da değişmiş kayıt için uyarı + otomatik yeniden hesap. Çevrimdışı çok cihaz senaryosunda para kaybını bitirir. (orta–büyük, yüksek)
6. **Firestore belge boyutu koruması:** `canliYaz` öncesi `JSON.stringify(...).length > 900_000` ise yazma ve kullanıcıya net uyarı; fotoğraf/fişleri belgeden çıkarıp Cloudinary'yi zorunlu kılın ya da Storage'a geçin (storage.rules zaten hazır); `hareketler/satislar` için yıllık arşiv belgesi. (orta, yüksek)
7. **`ayar` belgesini alan bazlı yazın:** `set(...,{merge:true})` + yalnız değişen alanlar; `seq`'i ayrı belgeye alın (ya da tamamen cihaz-ekli/UUID id'ye geçip `seq`'i bırakın). (küçük, orta)
8. **CI ve statik kontrol:** GitHub Actions'ta 6 Playwright testi; `eslint --no-eslintrc --env browser --rule no-undef` (tek dosyada tanımsız fonksiyon çağrılarını yakalar); `APP_SURUM` ve `sw.js CACHE`'i tek kaynaktan üreten küçük bir script; para/stok hesaplarını saf fonksiyonlara ayırıp `node:test` ile birim test. (küçük, orta)
9. **Ajan hesapları:** yazıcı ve WhatsApp ajanlarına patron şifresi yerine `rol:'ajan'` olan ayrı bir hesap; kurallarda `yazdirma`/`wa_kuyruk` için bu rol; `config.json` şifresini Windows Credential Manager'dan okumak orta vadeli hedef. Firebase App Check ile API anahtarının başka sitelerden kullanımını kısın. (küçük–orta, orta)
10. **wa-agent'ta sıralı gönderim + bekleme + numara doğrulama** (`getNumberId`), hata mesajlarını kuyruk belgesine yazmaya devam. (küçük, orta)
11. **Portal şifrelerini kaldırın:** `cari.kullanici/sifre` yerine yalnız Firebase Auth; gerekiyorsa tek kullanımlık davet kodu. (küçük, orta)
12. **Katalog belgesinde gizli fiyat ve kupon sızıntısını kapatın:** `fiyatGizli` ürünlerde `satis:0`; kupon doğrulamasını müşteri tarafında değil onayda yapın (kod listesi herkese açık olmasın). (küçük, orta)
13. **`sessiz-yazici/*.bat`'ı CRLF yapın** ve `.gitattributes`'a rağmen LF kalan dosya olmaması için CI'da `git ls-files --eol` kontrolü. (küçük, düşük)
14. **README'yi yazın, YAYINLAMA.md'yi kurallarla senkronlayın** (sahip e-postası, "Bu hesabı yönetici yap", ajan kurulumu, test komutları). (küçük, düşük)
15. **Uzun vade — modülerleştirme:** index.html'i en azından `veri.js` (model/senkron/birleştirme), `para.js` (hesaplar), `ui/*.js` olarak bölen bir derleme adımı (esbuild tek komut). 825 KB tek dosya her değişiklikte tüm uygulamayı riske atıyor ve test yazmayı zorlaştırıyor. (büyük, orta)
